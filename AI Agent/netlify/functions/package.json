{
  "name": "seatalk-lowstock-function",
  "description": "Cloud Function pengirim notifikasi SeaTalk saat Stock Asset menipis",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true
}

{
  "_comment": "Tambahkan 'firebase-admin' ke dependencies di package.json functions/ yang udah ada di repo lo (yang dipakai gemini-proxy.js). Jangan timpa file yang ada, cukup tambah baris ini ke object 'dependencies'.",
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}

